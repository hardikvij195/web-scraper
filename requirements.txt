playwright>=1.45
httpx>=0.27
curl_cffi>=0.7    ; optional, TLS-impersonation retry for WAF/Cloudflare 403s (see impersonate_fetch.py); enrichment degrades gracefully without it
selectolax>=0.3
typer>=0.12
rich>=13
python-dotenv>=1.0
phonenumbers>=8.13
tldextract>=5
fastapi>=0.110
uvicorn>=0.29
openpyxl>=3.1
psycopg2-binary>=2.9    ; optional, only for direct-Postgres Supabase (REST sync needs no extra deps)
pytest>=8
